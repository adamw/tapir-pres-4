package com.softwaremill.demo

object S170_Gains_and_losses {
  def gains(): List[String] = List(
    "abstraction",
    "flexibility",
    "separation of concerns",
    "reusability",
    "understandability"
  )

  def losses(): List[String] = List(
    "the tapir API isn't yet complete",
    "filters / middleware",
    "websockets / file serving"
  )

  def goals(): List[String] = List(
    "type-safe",
    "re-usable values",
    "programmer-friendly types",
    "explorable"
  )
}
