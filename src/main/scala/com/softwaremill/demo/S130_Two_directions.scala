package com.softwaremill.demo

class S130_Two_directions {

  def generate_code_from_openapi_yaml(): Unit = {
    /*
    pro: one format for multiple implementation languages
    cons: lack of precision, necessity to write yaml
    projects: guardrail
   */
  }

  def generate_openapi_yaml_from_code(): Unit = {
    /*
    pro: using a real programming language, flexibility, expressiveness
    cons: not all openapi features are covered
    projects: endpoints, typedapi, rho, typed-schema, tapir
   */
  }
}
