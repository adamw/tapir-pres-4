package com.softwaremill.demo

class S300_Shape_of_input {
  /*
  Two alternative inputs: Either[I1, I2], sealed trait family
interpreted as a client: how to match a value to a specific input?
complicates the API
little practical usage (no use-cases so far)
can be represented as a tuple of options + additional validation
   */
}
