package com.softwaremill.demo

class S370_Codecs {
/*
(almost) every library needs a notion of codec
  Encoders/Decoders in Circe
  Meta in Doobie
  (...)
One-way, or two-way?
  In tapir, two-way, because you never know how the endpoint is going to be interpreted
When to capture the codecs?
  When the endpoint is described
  When the endpoint is interpreted
 */
}
